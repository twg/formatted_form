<%=
case options[:builder].options[:type].to_s
when 'inline', 'search'
  render :partial => 'formatted_form/templates/field_inline', :locals => { :options => options }
else
  render :partial => 'formatted_form/templates/field_block', :locals => { :options => options }
end
%>